<% if logged_in? %>
    <% if micropost.liked_by?(current_user) %>
        <%= link_to micropost_likes_path(micropost), method: :delete, remote: true do %>
            <i class="fa fa-heart" aria-hidden="true" style="color: red;">
                <%= micropost.likes.count %>
            </i>&emsp;
        <% end %>
    <% else %>
        <%= link_to micropost_likes_path(micropost), method: :post, remote: true do %>
            <i class="fa fa-heart" aria-hidden="true" style="color: #C0C0C0;">
                <%= micropost.likes.count %>&emsp;
            </i>
        <% end %>
    <% end %>
<% else %>
    <i class="fa fa-heart" aria-hidden="true">
            <%= micropost.likes.count %>&emsp;
    </i>
<% end %>